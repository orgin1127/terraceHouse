spring:
  profiles: 
    active: local 

---
spring:
  h2:
    console:
      enabled: true
  jpa: 
    show-sql: true 
    hibernate: 
      ddl-auto: create-drop
  profiles: local 
  datasource: 
    data: classpath:data-h2.sql #

server:
 port: 8888
